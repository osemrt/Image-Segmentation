#include "header_files/declarations.h"

//A utility function to check that the given pixel
//is connected to any component or not
bool isConnected(bool** connected, int i, int j) {
	return connected[i][j];
}